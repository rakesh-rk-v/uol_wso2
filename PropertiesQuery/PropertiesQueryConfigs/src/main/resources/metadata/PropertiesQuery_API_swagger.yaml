openapi: 3.0.1
info:
  title: PropertiesQuery_API
  description: API Definition of PropertiesQuery_API
  version: 1.0.0
servers:
- url: http://localhost:8290/propertiesQuery_API
paths:
  /{queryName}:
    get:
      parameters:
      - name: queryName
        in: path
        required: true
        style: simple
        explode: false
        schema:
          type: string
      responses:
        default:
          description: Default response
